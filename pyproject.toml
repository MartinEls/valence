[project]
name = "valence"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Martin Elstner", email = "martin@elstner.dev" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
valence = "valence:main"

[build-system]
requires = ["uv_build>=0.10.7,<0.11.0"]
build-backend = "uv_build"
